<?xml version="1.0" encoding="utf-8"?>
<odoo>
<template id="report_mrporder">
    <t t-call="web.html_container">
        <t t-foreach="docs" t-as="o">
            <t t-call="web.internal_layout">
                <div class="page">
                    <div class="oe_structure"/>
                    <div class="row">
                        <div class="col-xs-12">
                            <h2><span t-field="o.name"/></h2>
                        </div>
                    </div>
                    <div class="row mt32 mb32">
                        <div class="col-xs-3" t-if="o.origin">
                            <strong>Source Document:</strong><br/>
                            <span t-field="o.origin"/>
                        </div>
                        <div class="col-xs-3">
                            <strong>Responsible:</strong><br/>
                            <span t-field="o.user_id"/>
                        </div>
                        <div class="col-xs-3">
                            <strong>Scheduled Date:</strong><br/>
                            <span t-field="o.date_planned_start"/>
                            <span t-if="o.date_planned_finished"> - <span t-field="o.date_planned_finished"/></span>
                        </div>
                    </div>

                    <div class="row mt32 mb32">
                        <div class="col-xs-3">
                            <strong>Finished Product:</strong><br/>
                            <span t-field="o.product_id"/>
                        </div>
                        <div class="col-xs-3">
                            <strong>Quantity to Produce:</strong><br/>
                            <span t-field="o.product_qty"/>
                            <span t-field="o.product_uom_id.name" groups="product.group_uom"/>
                        </div>
                        <div class="col-xs-3">
                            <strong>Printing date:</strong><br/>
                            <span t-esc="time.strftime('%Y-%m-%d')"
                                  t-options='{"widget": "date"}'/>
                        </div>
                    </div>

                    <div t-if="o.workorder_ids">
                        <h3>Planned Operations</h3>
                        <table class="table table-condensed">
                            <tr>
                                <td><strong>Operation</strong></td>
                                <td><strong>WorkCenter</strong></td>
                                <td><strong>No. Of Minutes</strong></td>
                            </tr>
                            <tr t-foreach="o.workorder_ids" t-as="line2">
                                <td><span t-field="line2.name"/></td>
                                <td><span t-field="line2.workcenter_id.name"/></td>
                                <td><span t-field="line2.duration_expected"/></td>
                            </tr>
                        </table>
                    </div>

                    <h3 t-if="o.move_raw_ids">
                        <t t-if="o.state == 'done'">
                            Products to Consumed
                        </t>
                        <t t-else="">
                            Products to Consume
                        </t>
                    </h3>

                    <table class="table table-condensed" t-if="o.move_raw_ids">
                        <t t-set="has_location" t-value="any(o.move_raw_ids.filtered(lambda x: x.move_line_ids.filtered(lambda x: x.location_id.id)))" groups="stock.group_stock_multi_locations"/>
                        <t t-set="has_serial_number" t-value="any(o.move_raw_ids.filtered(lambda x: x.move_line_ids.filtered(lambda x: x.lot_id or x.lot_name)))" groups="stock.group_production_lot"/>
                        <thead>
                            <tr>
                                <th>Product</th>
                                <th>Quantity</th>
                                <th t-if="has_location">From</th>
                                <th t-if="has_serial_number and o.state=='done'" class="h6">
                                    <strong>Lot/Serial barcode</strong></th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-if="o.move_raw_ids">
                                <t t-set="lines" t-value="o.move_raw_ids"/>
                                <t t-if="o.state == 'done'">
                                    <t t-set="lines"  t-value="o.move_raw_ids.filtered(lambda x: x.quantity_done)"/>
                                </t>
                                <t t-foreach="lines" t-as="line">
                                    <t t-foreach="line.move_line_ids" t-as="ml" t-if="o.state=='done'">
                                        <tr>
                                            <td>
                                                <t t-if="product_name != line.product_id.display_name">
                                                    <span t-field="line.product_id"/>
                                                </t>
                                                <t t-set="product_name" t-value="line.product_id.display_name"/>
                                            </td>
                                            <td>
                                                <span t-esc="ml.qty_done"/>
                                                <span t-field="line.product_id.uom_id" groups="product.group_uom"/>
                                            </td>
                                            <td t-if="has_location">
                                                <span t-field="line.location_id"/>
                                            </td>
                                            <td width="15%" t-if="has_serial_number">
                                               <t t-if="has_serial_number and (ml.lot_id or ml.lot_name)">
                                                    <img t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('Code128', ml.lot_id.name, 600, 100)" style="width:100%;height:35px"/>
                                                </t>
                                            </td>
                                        </tr>
                                    </t>
                                    <t t-if="o.state!='done'">
                                        <tr>
                                            <td>
                                                <span t-field="line.product_id"/>
                                            </td>
                                            <td>
                                                <span t-esc="line.product_uom_qty"/>
                                            </td>
                                            <td t-if="has_location">
                                                <span t-field="line.location_id"/>
                                            </td>
                                        </tr>
                                    </t>
                                </t>
                            </t>
                        </tbody>
                    </table>
                    <div class="oe_structure"/>
                </div>
            </t>
        </t>
    </t>
</template>

<template id="production_message">
    <t t-if="move.move_id.raw_material_production_id">
        <t t-set="message">Consumed</t>
    </t>
    <t t-if="move.move_id.production_id">
        <t t-set="message">Produced</t>
    </t>
    <strong><t t-esc="message"/> quantity has been updated.</strong>
</template>

<template id="track_production_move_template">
    <div>
        <t t-call="mrp.production_message"/>
        <t t-call="stock.message_body"/>
    </div>
</template>
</odoo>
